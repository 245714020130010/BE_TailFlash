spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tailflash_test_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: tailflash_user
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
